# ---------------------------------------------------------------
# Client environment — only needed when the API is hosted separately.
# On Vercel, set this under Project → Settings → Environment Variables.
# Leave unset locally: the Vite dev server proxies /api to Express.
# ---------------------------------------------------------------

# Base URL of the API, no trailing slash. Example: https://smc-api.onrender.com
VITE_API_URL=
